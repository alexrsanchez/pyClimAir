Installation
============

You can install pyClim with the following command:

.. code:: python
    
    pip install pyclim

For testing purposes, you can also install from TestPyPI:

.. code:: python

    pip install -i https://test.pypi.org/simple/ pyclim_test



System requirements
-------------------
- Python 3.12 or above.

- The package is compatible with major operating systems (Linux, macOS, Windows).
